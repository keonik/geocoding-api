# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
geocoding-api

# frontend static output
static-new/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Environment variables
.env

# Air temporary files
tmp/
build-errors.log

# Downloaded data cache
cache/

# Uploaded datasets directory (persisted via Docker volume)
uploads/*
!uploads/.gitkeep

# Ohio county data files (now managed via Data Manager uploads)
oh/*
!oh/.gitkeep

# Core reference data (keep compressed versions for initial setup)
georef-united-states-of-america-zc-point.csv
!georef-united-states-of-america-zc-point.csv.gz

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Documentation build artifacts
docs/dist/
node_modules/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# troubleshooting logs
*.log